# Gliding Navigation Initial Review

Desktop route checks confirmed that the selected Home and One-Pager states render as dark active controls. The first isolation pass used margins only, which left the continuous glass rail visually intact. The active-selection layer now carries a page-ground knockout shadow so the margin reads as a true visible break from the surrounding track; the selected label remains dark-styled in light mode rather than returning to the earlier white active treatment.
