# One-Pager anchor preview status

On 2026-08-27, browser navigation to `#freshness` and `#system` confirmed the requested anchors in the current source but exposed that the running preview still served a stale bundle without the contents rail. A clean development-server restart is required before the final click-through anchor validation.
