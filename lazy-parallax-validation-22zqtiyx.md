# Lazy Loading and Parallax Validation

Below-the-fold Suite illustrations, lower Home marks, hardware logos, final CTA infrastructure artwork, and lower document imagery now use native `loading="lazy"` with asynchronous decoding. Above-the-fold identity marks and the opening media remain eager so the first view is not delayed. The Home cinematic background and an independent halo move through small composited transforms derived from existing scroll progress; reduced-motion visitors receive static layers. Desktop and 390px mobile checks retained the initial visual hierarchy without overflow.
