# Footer Motto Animation Validation

The Terms route renders the shared `CortexFooter`, confirming that the same footer component reaches the legal document path. The new motto graphic is attached to the footer brand lockup and is configured to reveal only when its own viewport observer detects entry into view. Motion is bypassed for visitors who request reduced motion.
